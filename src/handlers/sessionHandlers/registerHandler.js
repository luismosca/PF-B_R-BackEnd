const { registerController } = require("../../controllers/sessionControllers/registerController")

const registerHandler = async (req, res) => {
    try {
        const {name_surName, email, password, image} = req.body
        if (!name_surName, !email, !password) {
            
        }
    } catch (error) {
        
    }
}